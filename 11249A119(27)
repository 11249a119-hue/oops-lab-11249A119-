EXP NO :27

AIM:
To implement and demonstrate the use of class templates in C++.

ALGORITHM:
Start the program.
Define a class template using the template keyword with a generic data type T.
Create data members and member functions inside the template class.
Write functions to perform simple operations (e.g., addition, comparison, etc.).
In the main() function, create objects of the class with different data types (like int, float, etc.).
Display the results.
End the program.

PROGRAM:
#include <iostream>
using namespace std;

// Class Template Definition
template <class T>
class Calculator {
    T num1, num2;

public:
    Calculator(T n1, T n2) {
        num1 = n1;
        num2 = n2;
    }

    void displayResult() {
        cout << "Numbers are: " << num1 << " and " << num2 << endl;
        cout << "Addition = " << add() << endl;
        cout << "Subtraction = " << subtract() << endl;
        cout << "Multiplication = " << multiply() << endl;
        cout << "Division = " << divide() << endl;
    }

    T add()        { return num1 + num2; }
    T subtract()   { return num1 - num2; }
    T multiply()   { return num1 * num2; }
    T divide()     { return num1 / num2; }
};

int main() {
    cout << "--- Integer Calculator ---" << endl;
    Calculator<int> intCalc(10, 5);
    intCalc.displayResult();

    cout << "\n--- Float Calculator ---" << endl;
    Calculator<float> floatCalc(7.5, 2.5);
    floatCalc.displayResult();

    return 0;
}

OUTPUT:
Integer Calculator
Numbers are: 10 and 5
Addition = 15
Subtraction = 5
Multiplication = 50
Division = 2

Float Calculator 
Numbers are: 7.5 and 2.5
Addition = 10
Subtraction = 5
Multiplication = 18.75
Division = 3

RESULT:
The program successfully demonstrates the use of a class template to perform arithmetic operations for multiple data types in C++.
